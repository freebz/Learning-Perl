# use strict 프라그마

$bamm_bamm = 3;    # 펄은 $bamm_bamm 변수를 자동으로 생성합니다.


$bammbamm += 1;    # 어이쿠!


use strict;    # 좋은 프로그래밍 규칙 몇 가지를 강제적으로 적용하도록 합니다.


my $bamm_bamm = 3;    # 새로운 렉시컬 변수


$bammbamm += 1;    # 해당하는 변수가 존재하지 않습니다.
